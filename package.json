{
  "name": "ts-bolt-app",
  "scripts": {
    "build": "tsc -p .",
    "start": "yarn run build && node dist/app.js"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^15.0.3",
    "typescript": "^4.2.4"
  }
}
